services:
  - type: web
    name: churn-predictor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: PYTHONPATH
        value: .
      - key: FLASK_ENV
        value: production 